import styled from 'styled-components';

export const Container = styled.div`
  color: #fff;
  li {
    list-style: none;
  }
  ul {
    display: flex;
    justify-content: center;
    padding-left: 0;
  }
`;
